/**
 * Transform raw get_markets_page() response into typed data.
 * For now, passes through the raw data — to be refined when
 * the exact response shape is confirmed on-chain.
 */
export function formatMarketsPage(raw: any) {
  return raw;
}
